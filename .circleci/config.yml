version: 2
jobs:
  build:
    working_directory: ~/learning
    docker:
      - image: cimg/ruby:3.1.2-browsers
      - environment:
          RAILS_ENV: test
    steps:
      - checkout
      - run: gem install bundler
      - run: bundle exec rake
